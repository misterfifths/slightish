Some prose, and then

```sh
$ some scripts
| with their results

$ and another
```

And later, perhaps

```sh
$ a third \
with a multiline $(echo "command")
@ and `some shell expansions` like $PATH
@ and also ${HOME}
```
